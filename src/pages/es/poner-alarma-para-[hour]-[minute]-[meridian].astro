---
/**
 * SEO Pages for Alarm Clock (Spanish)
 * Generates static pages for each hour/minute/meridian combination
 * Example: /es/poner-alarma-para-7-00-am/
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import AlarmClock from '../../components/alarm/AlarmClock.astro';
import { t } from '../../scripts/i18n';

// Generate all possible alarm time combinations
export async function getStaticPaths() {
  const paths = [];
  const hours = Array.from({ length: 12 }, (_, i) => i + 1);
  const minutes = [0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55];
  const meridians = ['am', 'pm'] as const;

  for (const hour of hours) {
    for (const minute of minutes) {
      for (const meridian of meridians) {
        paths.push({
          params: {
            hour: hour.toString(),
            minute: minute.toString().padStart(2, '0'),
            meridian: meridian,
          },
        });
      }
    }
  }

  return paths;
}

const { hour, minute, meridian } = Astro.params;
const locale = 'es';

const hourNum = parseInt(hour || '7');
const minuteNum = parseInt(minute || '0');
const meridianUpper = (meridian || 'am').toUpperCase() as 'AM' | 'PM';

const timeString = `${hour}:${minute} ${meridianUpper}`;
const title = `Poner Alarma para ${timeString} - Reloj Despertador en Línea`;
const description = `Configura una alarma gratis para ${timeString}. Despierta a tiempo con nuestro reloj despertador confiable. Sin registro requerido, funciona en tu navegador.`;
const keywords = `poner alarma para ${timeString}, alarma ${hour}${meridian}, despertar a las ${timeString}, alarma online ${timeString}`;
---

<BaseLayout title={title} description={description} keywords={keywords}>
  <div class="container mx-auto px-4 py-12">
    <div class="text-center mb-12">
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-4">
        Poner Alarma para {timeString}
      </h1>
      <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
        {description}
      </p>
    </div>

    <AlarmClock
      presetHour={hourNum}
      presetMinute={minuteNum}
      presetMeridian={meridianUpper}
    />

    <div class="max-w-4xl mx-auto mt-12 prose dark:prose-invert">
      <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">
        Cómo Poner una Alarma para {timeString}
      </h2>

      <div class="space-y-4 text-gray-600 dark:text-gray-400">
        <p>
          Configurar una alarma para las <strong>{timeString}</strong> es fácil con nuestro reloj despertador en línea gratuito.
          Simplemente haz clic en el botón "Poner Alarma" arriba, y tu alarma estará lista para despertarte exactamente a las {timeString}.
        </p>

        <h3 class="text-xl font-semibold text-gray-900 dark:text-white mt-6 mb-3">
          Características:
        </h3>
        <ul class="list-disc list-inside space-y-2">
          <li>Sonidos de alarma personalizables</li>
          <li>Opción de repetir alarma para despertares diarios</li>
          <li>Funciona en tu navegador - sin necesidad de descargar</li>
          <li>Temporización confiable y precisa</li>
          <li>Notificaciones del navegador cuando suena la alarma</li>
          <li>Títulos y etiquetas personalizadas para alarmas</li>
        </ul>

        <h3 class="text-xl font-semibold text-gray-900 dark:text-white mt-6 mb-3">
          ¿Por Qué Usar un Reloj Despertador en Línea?
        </h3>
        <p>
          Nuestro reloj despertador en línea es perfecto cuando necesitas un despertador confiable. Ya sea que
          estés tomando una siesta, necesites despertar para trabajar, o tengas una reunión importante, configurar una alarma para las {timeString}
          asegura que no te quedarás dormido.
        </p>

        <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 mt-6">
          <p class="text-sm text-blue-900 dark:text-blue-100 mb-0">
            <strong>💡 Consejo:</strong> Mantén esta pestaña abierta y tu computadora encendida para que la alarma funcione correctamente.
            También puedes marcar esta página para configurar rápidamente una alarma para las {timeString} en cualquier momento.
          </p>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
